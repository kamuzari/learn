spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: false
  redis:
    port: 6379
    host: localhost

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace